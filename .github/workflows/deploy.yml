name: 🚀 Deploy Fullstack to Apache2 VM 

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # ----------------- CHECKOUT -----------------
      - name: 🧩 Checkout código
        uses: actions/checkout@v4

      # ----------------- NODE -----------------
      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # ----------------- DEPENDENCIAS -----------------
      - name: 💡 Instalar npm-run-all y Angular CLI
        run: |
          npm install --save-dev npm-run-all
          npm install -g @angular/cli

      - name: 📦 Instalar dependencias (todo el repo)
        run: npm run install:all

      # ----------------- BUILD FRONTEND -----------------
      - name: 🏗️ Build frontend
        run: |
          cd front-end
          ng build --configuration production --base-href /jose_site/
          cd ..

      # ----------------- BUILD BACKEND -----------------
      - name: 🏗️ Build backend
        env:
          NODE_ENV: production
          DB_TYPE: ${{ secrets.DB_TYPE }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          PORT: ${{ secrets.PORT }}

        run: |
          cd back-end
          npm run build

      # ----------------- AJUSTAR BASE HREF -----------------
      - name: 🧩 Ajustar ruta base en index.html
        run: |
          sed -i 's|<base href="/jose_site/">|<base href="/jose_site/deploy-frontend/">|' front-end/dist/jose_site/browser/index.html

      # ----------------- PREPARAR FRONTEND -----------------
      - name: 🧩 Preparar archivos frontend
        run: |
          mkdir -p deploy-frontend
          cp -r front-end/dist/jose_site/browser/* deploy-frontend/
          cp front-end/dist/jose_site/3rdpartylicenses.txt deploy-frontend/ || true
          cp front-end/dist/jose_site/prerendered-routes.json deploy-frontend/ || true

      # ----------------- PREPARAR BACKEND -----------------
      - name: 🧩 Preparar archivos backend
        run: |
          mkdir -p deploy-backend
          cp -r back-end/dist/* deploy-backend/

      # ----------------- DEBUG -----------------
      - name: 🧾 Ver contenido listo para deploy
        run: |
          echo "=== FRONTEND ==="
          ls -R deploy-frontend || true
          echo "=== BACKEND ==="
          ls -R deploy-backend || true

      # ----------------- UPLOAD FRONTEND -----------------
      - name: 📤 Subir frontend al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          source: "deploy-frontend/**"
          target: "/var/www/jose_site/"
          overwrite: true

      # ----------------- UPLOAD BACKEND -----------------
      - name: 📤 Subir backend al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          source: "deploy-backend/**"
          target: "/var/www/jose_site/deploy-backend/"
          overwrite: true

      # ----------------- SSH TASKS -----------------
      - name: ⚙️ Configurar permisos y reiniciar PM2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          script: |
            echo "✅ Archivos subidos correctamente"
            chmod -R 775 /var/www/jose_site
            chmod -R 775 /var/www/jose_site/deploy-backend

            # Reiniciar PM2 si existe
            if command -v pm2 >/dev/null 2>&1; then
              cd /var/www/jose_site/deploy-backend
              pm2 start main.js --name backend --update-env || pm2 restart backend
              pm2 save
            else
              echo "⚠️ PM2 no está instalado en el servidor"
            fi
