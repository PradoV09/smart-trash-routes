name: 🚀 Deploy Fullstack to Apache2 VM

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # ----------------- CHECKOUT -----------------
      - name: 🧩 Checkout código
        uses: actions/checkout@v4

      # ----------------- NODE -----------------
      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # ----------------- DEPENDENCIAS -----------------
      - name: 💡 Instalar npm-run-all y Angular CLI
        run: |
          npm install --save-dev npm-run-all
          npm install -g @angular/cli

      - name: 📦 Instalar dependencias (todo el repo)
        run: npm run install:all

      # ----------------- BUILD FRONTEND -----------------
      - name: 🏗️ Build frontend (solo browser)
        run: |
          cd front-end
          ng build --configuration production --base-href /jose_site/
          cd ..

      # ----------------- BUILD BACKEND -----------------
      - name: 🏗️ Build backend
        run: |
          cd back-end
          npm run build || echo "⚠️ No build script, se usará código fuente"
          cd ..

      # ----------------- DEBUG: VERIFICAR DIST -----------------
      - name: 🧾 Ver contenido del dist antes de subir
        run: |
          echo "=== FRONTEND DIST ==="
          ls -R front-end/dist/front-end/browser || true
          echo "=== BACKEND DIST ==="
          ls -R back-end/dist || true

      - name: 📤 Subir frontend (solo browser) al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          source: "front-end/dist/front-end/browser/**"
          target: "/var/www/jose_site/frontend/"
          overwrite: true

      # ----------------- UPLOAD BACKEND -----------------
      - name: 📤 Subir backend al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          source: "back-end/dist/**"
          target: "/var/www/jose_site/backend/"
          overwrite: true

      # ----------------- SSH TASKS -----------------
      - name: ⚙️ Configurar permisos y reiniciar PM2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          script: |
            echo "✅ Archivos subidos correctamente"
            chmod -R 775 /var/www/jose_site/frontend
            chmod -R 775 /var/www/jose_site/backend

            # Reiniciar PM2 si existe
            if command -v pm2 >/dev/null 2>&1; then
              cd /var/www/jose_site/backend
              pm2 start main.js --name backend --update-env || pm2 restart backend
              pm2 save
            else
              echo "⚠️ PM2 no está instalado en el servidor"
            fi
